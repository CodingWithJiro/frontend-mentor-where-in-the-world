const SkipLink = () => {
  return (
    <a className="sr-only" href="#main" aria-label="Skip to main content">
      Go directly to the main content of the page.
    </a>
  );
};

export default SkipLink;
